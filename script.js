function tampilkanAlert() {
  const nama = document.getElementById("nama").textContent;
  const kampus = document.getElementById("kampus").textContent;
  alert("Halo, saya " + nama + " dari " + kampus);
}

console.log("File JS sudah terhubung dengan benar.");